import styled from 'styled-components';

export const List = styled.ul`
  list-style: none;
  margin-left: auto;
  margin-right: auto;
  padding-left: 0;
`;
export const Title = styled.h1`
  text-align: center;
`;
